kind = "web"
previewPath = "/"
title = "Adsrahu - Digital Marketing Agency"
version = "1.0.0"
id = "artifacts/adsrahu"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23360

[services.development]
run = "pnpm --filter @workspace/adsrahu run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/adsrahu", "run", "build" ]
publicDir = "artifacts/adsrahu/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23360"
BASE_PATH = "/"
